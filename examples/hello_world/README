This directory contains a minimal hello world example to test reopt.

The Makefile uses clang-3.5, and has the ability to build with both
the default libc and musl (by specifying MUSL_BASE to set the path).
